name: Feature request
description: Suggest a new feature or improvement for Patternaut.
title: '[Feature] '
labels: ['enhancement']
body:
  - type: markdown
    attributes:
      value: |
        Thanks for the suggestion. Patternaut is deliberately focused, so the clearer the problem you're trying to solve, the easier it is to evaluate. "Not right now" is a normal answer — it's not a judgement of the idea.

  - type: textarea
    id: problem
    attributes:
      label: What problem are you trying to solve?
      description: Describe the situation or pain point. Skip mentioning a solution here.
    validations:
      required: true

  - type: textarea
    id: proposal
    attributes:
      label: Your proposed solution (optional)

  - type: textarea
    id: alternatives
    attributes:
      label: Alternatives you've considered (optional)

  - type: checkboxes
    id: alignment
    attributes:
      label: Alignment checks
      description: These keep requests inside the product DNA (native, pay-once, private, focused).
      options:
        - label: This does not require an account, cloud sync, or sending data off-device
          required: true
        - label: This does not involve subscriptions, in-app purchases, or ad revenue
          required: true
        - label: I searched existing issues and didn't find a duplicate
          required: true
